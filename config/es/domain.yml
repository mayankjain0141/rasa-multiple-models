version: "2.0"

intents:
  - greet
  - goodbye

responses:
  utter_greet:
  - text: "Hola! Como estas?"

  utter_goodbye:
  - text: "Hasta luego!"

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
